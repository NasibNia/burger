<li>
    {{this.burger_name}}
    <button class="change-stat btn-primary" data-id="{{this.id}}" data-newStat="{{devour}}" data-name ="{{this.burger_name}}">
        {{#if devour}}YUCK!{{else}}YUMM!{{/if}}
    </button>

</li>
